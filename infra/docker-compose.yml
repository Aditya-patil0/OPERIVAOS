services:
  postgres:
    image: postgres:16
    container_name: operiva-postgres
    environment:
      POSTGRES_PASSWORD: test
      POSTGRES_DB: operiva
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
